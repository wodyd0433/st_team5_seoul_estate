# 추천 요약 값 검증 문서

이 문서는 추천 요약에 표시되는 값이 가상 데이터인지 여부와 계산 근거를 검증하기 위한 문서다. 현재 추천 요약과 페르소나 시뮬레이션에서 사용하는 전세, 월세, 통근, 평수, 매매가 관련 값은 모두 원천 데이터 또는 원천 데이터를 자치구 단위로 집계한 파생값이며, 임의 생성값은 사용하지 않는다. 다만 페르소나의 월소득 추정, 부채 추정, 종잣돈, 2년 후·3년 후 매수 가능 예산은 집단 통계를 기반으로 만든 시뮬레이션 값이므로 원천 실측값과 구분해서 해석해야 한다.

`전세 보증금`은 `seoul_apt_rent_5y.csv`에서 선택한 평형 구간과 연도에 해당하는 거래를 자치구별로 모은 뒤 `보증금_만원_krw` 중앙값으로 계산한다. `월세`는 같은 데이터에서 월세가 0보다 큰 거래만 따로 모아 `월세_만원_krw` 중앙값을 계산한 `monthly_rent_active_krw`를 사용한다. 따라서 추천 카드의 월세는 “월세가 실제로 존재한 거래의 대표값”이며, 전세 계약과 월세 계약을 섞어서 평균낸 값이 아니다.

`평수`와 `면적`은 사용자가 사이드바에서 선택한 평형 구간에서 온다. 화면에는 `selected_area_min_m2`, `selected_area_max_m2`, `selected_area_min_pyeong`, `selected_area_max_pyeong`을 사용해 표시한다. 이 값은 추천 결과를 계산할 때 먼저 원천 거래 데이터를 해당 면적 범위로 필터링한 뒤 남은 거래를 기준으로 집계하기 때문에, 표시용 장식값이 아니라 실제 필터 조건 자체다.

`통근시간`은 원천 통근 CSV 네 개를 사용해 허브별 회귀계수를 만든 뒤, 자치구 중심 좌표와 직장 허브 좌표 사이 직선거리와 평균 환승 수를 이용해 추정한다. 1인 가구는 `commute_minutes`를, 2인 맞벌이는 `primary_commute_minutes`, `secondary_commute_minutes`, `worst_commute_minutes`를 함께 표시한다. 즉 2인 가구의 추천 요약에는 두 사람 통근시간이 각각 따로 나오며, 하나의 평균값만 보여주지 않는다.

`대표 매매가`는 `apt_deal_total.csv`에서 같은 평형 구간을 적용한 뒤 자치구별 `dealAmount_krw` 중앙값으로 계산한다. 페르소나 시뮬레이션의 `2년 후 매수 가능성`, `3년 후 매수 가능성`은 이 대표 매매가와 `buying_power_2y_krw`, `buying_power_3y_krw`의 차이(`buy_2y_gap_krw`, `buy_3y_gap_krw`)로 판정한다. 따라서 “매수 가능”, “도전 가능”, “어려움” 분류는 대표 매매가 대비 여유 자금 차이를 기준으로 한 분류이며, 실제 대출 심사나 개별 매물 가격을 그대로 반영한 값은 아니다.

결론적으로 추천 요약의 주거비, 통근, 평형 값은 가상 생성 데이터가 아니며 원천 데이터 집계값이다. 반면 페르소나 재무값과 향후 매수 가능성은 `DT_1NW1027.csv`와 `debt_newlyweds.csv`를 바탕으로 한 통계적 시뮬레이션 결과다. 이 구분을 화면과 문서에서 계속 유지해야 하며, 추후 계산식을 바꾸더라도 어떤 값이 원천값이고 어떤 값이 가정 기반 값인지 반드시 문서에 같이 기록해야 한다.
